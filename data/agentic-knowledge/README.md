# Agentic Knowledge Base

`make agentic` writes `knowledge.jsonl` here.

The rows are generated from the radare2 source tree configured by `R2_SOURCE`.
They are not mixed into the manual TSV review flow. Paths in this dataset are
stored relative to `R2_SOURCE` so the files remain portable across machines.
