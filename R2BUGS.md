# R2BUGS

Potential radare2 source bugs and source-audit leads found by agentic scans.
Confirmed bugs should include a reproducer, impact notes, and fix status.

<!-- agentic-r2bugs:start -->
This block is generated by `make agentic`. Keep manual notes outside the markers.
Source bug findings are intentionally kept out of the training knowledge base.

## shell-command-injection

Status: unconfirmed source-audit lead. This is not a vulnerability claim until a reproducer or patch proves the risk.
Stable ref: `radare2-audit:shell-command-injection`
Pattern: `\br_sys_cmd(?:f|_str|_strf|dbg)?\b`
Current scan: 53 signals across 27 files.

Stable signal summary:
  Observed API variants: r_sys_cmdf, r_sys_cmd, r_sys_cmd_strf, R_API, r_sys_cmd_str, R_PRINTF_CHECK, R_SYS_DEVNULL.
  Observed sink markers: format-%s.

Audit guidance: Audit shell sinks for sandbox checks and shell escaping. Treat paths, URLs, package names, editor commands, and environment values as attacker-controlled until proven otherwise.

Verification required before fixing or filing:
- Re-run the scan on the target radare2 checkout.
- Trace whether user-controlled or binary-controlled input reaches the sink.
- Confirm the behavior with a minimal r2/r2r reproducer or a narrowly scoped source patch.

## script-output-escaping

Status: unconfirmed source-audit lead. This is not a vulnerability claim until a reproducer or patch proves the risk.
Stable ref: `radare2-audit:script-output-escaping`
Pattern: `\br_cons_printf\s*\(`
Current scan: 134 signals across 48 files.

Stable signal summary:
  Observed API variants: r_cons_printf, r_cons_gotoxy, r_str_get, R_CONS_CLEAR_LINE, r_buf_read8.
  Observed sink markers: format-%s, formatted-output.

Audit guidance: Audit printed output that may be consumed by star commands, generated scripts, projects, or command replay. Binary-controlled strings should be filtered with the sanitizer matching the output language.

Verification required before fixing or filing:
- Re-run the scan on the target radare2 checkout.
- Trace whether user-controlled or binary-controlled input reaches the sink.
- Confirm the behavior with a minimal r2/r2r reproducer or a narrowly scoped source patch.

## todo-memory-safety

Status: unconfirmed source-audit lead. This is not a vulnerability claim until a reproducer or patch proves the risk.
Stable ref: `radare2-audit:todo-memory-safety`
Pattern: `\b(?:TODO|XXX)\b.*\b(?:leak|overflow|bounds|sanitize|crash|NULL|null|memory|uaf|free)\b`
Current scan: 25 signals across 19 files.

Stable signal summary:
  Observed API variants: R_ESIL_OP_TYPE_UNKNOWN, r_core_visual_tab_free, r_esil_set_op, r_graph_free, r_main_radare2.
  Observed sink markers: XXX, TODO, format-%s.
  Representative signal shapes: note=// TODO: this is very slow and this is because using sdb for the agraph. keys is totally not efficient for memory and ab; note=// XXX core is null here cant work; note=// XXX double free - r_graph_free (g->graph);; note=// TODO: differentiate analysis by map ranges (associated with files or memory maps).

Audit guidance: Audit the TODO/XXX note as a lead only. Confirm ownership, bounds, nullability, and test coverage before treating it as a bug.

Verification required before fixing or filing:
- Re-run the scan on the target radare2 checkout.
- Trace whether user-controlled or binary-controlled input reaches the sink.
- Confirm the behavior with a minimal r2/r2r reproducer or a narrowly scoped source patch.
<!-- agentic-r2bugs:end -->
